# RE-FIT 제품 기획 — 환자용 앱 + 치료사용 웹

작성일: 2026-08-05 (2판, 게임 설계 확정 반영)
범위: MVP. 인허가 검토는 제작 이후.

---

## 0. 확정 사항

| 항목 | 결정 |
|---|---|
| 제품 구성 | RE-FIT Play(앱, 환자) + RE-FIT Care(웹, 치료사·관리자) |
| 대상 | **완전마비 환자** — 자발적 수의 수축 없음 |
| 게임 입력 | FES로 유발된 근수축을 EMG로 실시간 감지 → open/closed 판정 |
| 게임 목적 | 신경가소성 — 화면 동작 큐에 맞춰 쥠/폄을 지루하지 않게 반복 |
| 피로 게이지 계산 | 앱(Flutter) |
| 자동 종료 기준 | **적응형 baseline** 기준 게이지 |
| 자극 트리거 | 펌웨어 송신 불가 → **앱이 raw에서 주기 추정 후 위상 고정**(세션 시작 30초 동기화 구간) |
| 웹 | 사후 조회 + 실시간 관찰 + **원격 중단** |
| 환자–치료사 연결 | 초대 코드 |
| 백엔드 | 없음 → Supabase(Postgres) 신규 구축 |

**제품 분리 원칙: 앱은 상태를 보여주고, 웹은 숫자를 보여준다.**
기존 합본 앱의 모니터링 화면(파형, RMS/MDF 그래프)은 전부 웹으로 이관한다. 앱에는 환자가 보고 행동할 수 있는 것만 남긴다 — 전극을 다시 붙일지, 강도를 조정할지, 오늘은 끝낼지.

---

## 1. 게임 설계

### 1.1 원리 — 이 게임이 무엇을 하는 물건인가

완전마비 환자는 스스로 손을 쥘 수 없다. 손을 쥐게 하는 것은 FES이고, 앱이 하는 일은 **그 수축이 실제로 일어났는지 EMG로 확인하고, 그 순간을 화면과 시간적으로 일치시키는 것**이다.

신경가소성은 세 가지가 같은 시점에 겹칠 때 생긴다.

1. **운동 의도** — 화면의 손이 쥐어지는 걸 보며 "쥔다"고 생각하는 순간
2. **실제 움직임** — FES가 근육을 수축시키는 순간
3. **감각 되먹임** — 손이 쥐어졌다는 고유수용감각이 올라오는 순간

셋이 어긋나면 그냥 수동 운동이고, 겹치면 Hebbian 학습이 일어난다. **따라서 이 게임의 기술적 핵심은 재미가 아니라 타이밍 정확도다.** 화면 큐가 자극보다 늦게 나오면 훈련 효과가 사라진다.

### 1.2 코어 루프

자극 1버스트 = 쥠/폄 1회 = 게임 액션 1회. 실측 버스트 구조가 게임 템포를 그대로 결정한다.

```
[큐 선행 300ms]  화면의 손이 쥐어지기 시작 → 환자가 "쥔다"고 의도
      ↓
[자극 ON 591ms]  FES 발화 → M-wave 검출 → 실제 수축 확인 → 화면 손 완전히 쥠 + 햅틱/사운드
      ↓
[자극 OFF 1027ms] 이완 → 화면 손 펴짐 → 다음 큐 준비
      ↓
총 주기 1618ms — 분당 약 37회, 10분 세션에 약 340회 반복
```

실측 88세션 29,611버스트 기준 세션당 337버스트(중앙 9.3분)와 정확히 맞는다. 즉 **한 세션이 곧 약 340회 반복 훈련**이다.

### 1.3 자극 주기 동기화 (트리거 신호 없이)

펌웨어가 자극 시점을 알려주지 못하므로 앱이 raw 신호에서 직접 찾는다.

| 단계 | 내용 |
|---|---|
| 1. 검출 | 절대값 임계 + 불응기 그룹 argmax로 자극 아티팩트 후보 추출 (기존 오프라인 분석과 동일 방식) |
| 2. 주기 추정 | 자기상관으로 반복 주기 확정. 실측 1618ms 근방을 사전값으로 사용 |
| 3. 위상 고정 | 첫 자극 시점을 기준으로 UI 타임라인을 phase-lock |
| 4. 드리프트 보정 | 매 N버스트마다 검출된 실제 자극 시점으로 위상 재정렬 |

**세션 시작 30초는 동기화 구간**이다. "기기 신호를 맞추고 있어요" 안내를 띄우되, 이 구간을 A_ref 워밍업과 겹쳐 **튜토리얼 라운드로 포장**한다. 별도 대기 화면을 만들면 환자가 이탈한다.

> **최대 기술 리스크가 여기다.** ESP32 타이머와 폰 시계가 독립적으로 흐르므로 위상이 서서히 밀린다. 밀린 만큼 큐와 실제 수축이 어긋나고, 어긋나면 1.1의 신경가소성 조건이 깨진다. 드리프트 보정 주기와 허용 오차(±100ms 이내 권장)를 P0에서 반드시 실측으로 정해야 한다.

### 1.4 open / closed 판정

| 상태 | 판정 |
|---|---|
| **closed (쥠)** | 해당 버스트의 M-wave p2p ≥ 임계 (`A_ref × k`, k는 0.3 근방에서 튜닝) |
| **open (폄)** | 자극 OFF 구간 |
| **미수축 (실패)** | 자극은 나갔는데 p2p가 임계 미달 → 전극 접촉 불량 또는 강도 부족 |

자극이 나가도 수축이 안 일어나는 경우가 실제로 있다. 그래서 자극 주기만 믿고 화면을 움직이면 안 되고, **EMG로 확인된 것만 "성공"으로 처리**한다. 이 성공/실패 판정이 게임 점수이자 동시에 부착 품질 지표가 된다.

### 1.5 피로와 게임의 결합 — 별도 게이지 은유가 필요 없다

피로가 진행되면 M-wave 진폭이 떨어지고, 진폭이 떨어지면 1.4의 성공 판정이 실패하기 시작한다. 즉 **피로는 게임 안에서 "손이 점점 잘 안 쥐어진다"로 저절로 표현된다.**

| 게이지 구간 | 게임 내 표현 |
|---|---|
| 0–15% | 손이 확실하게 쥐어짐. 성공 이펙트 강함 |
| 15–30% | 정상 진행 |
| 30–45% | 쥐는 동작이 약해 보임. "슬슬 힘드시죠?" |
| 45%+ | 자동 종료 → "오늘 목표 달성" |

앱 화면에는 퍼센트 숫자를 띄우지 않는다. 숫자는 웹에만 있다.

**종료는 실패가 아니라 완주다.** 화면 문구, 사운드, 뱃지 전부 이 프레임을 지킨다. 피로 도달이 "졌다"로 읽히면 다음 날 안 켠다.

### 1.6 고정 템포라는 제약과 그 안에서의 재미

이 게임은 **속도를 바꿀 수 없다.** 템포가 자극 주기 1618ms에 묶여 있어서 리듬게임처럼 난이도를 올리는 방식이 원천적으로 불가능하다. 재미는 다른 축에서 만들어야 한다.

| 축 | 방법 |
|---|---|
| 시각 변주 | 스테이지마다 배경·캐릭터·오브젝트 교체. 같은 340회여도 다르게 보이게 |
| 목표 변주 | "오늘은 100번 물주기" / "50개 수확" — 반복 횟수에 매번 다른 의미 부여 |
| 서사 진행 | 세션마다 이야기가 조금씩 진행. 다음 세션을 켜는 이유가 됨 |
| 즉각 피드백 | 매 성공마다 사운드 + 햅틱 + 이펙트. 340번의 작은 성공 경험 |
| 누적 표현 | 세션 종료 시 "오늘 340번 쥐었어요" — 숫자 자체가 보상 |

동작은 MVP에서 **전체 손 쥠/폄 한 종류**다. 손가락을 개별 제어하려면 다채널 자극이 필요하므로 범위 밖이다. 화면에서는 손가락이 접히는 모습을 보여주되, 실제 자극은 전체 grasp 한 채널이다.

### 1.7 확장 여지 (MVP 이후)

잔존 수의 능력이 있는 불완전마비 환자에게는 **EMG-triggered FES**(환자가 쥐려고 시도 → 미세 EMG 감지 → 그 순간 자극 발화)가 신경가소성 측면에서 가장 강력한 방식이다. 지금 만드는 판정 로직(임계 기반 수축 검출)이 그대로 재활용된다. 완전마비 대상 MVP에서는 불가능하므로 큐 기반으로 간다.

---

## 2. 피로 게이지 — 적응형 단일 기준

### 2.1 공식

```
게이지%(t) = clamp( 100 × (1 − EMA(p2p_burst)(t) / A_ref_구간) , 0, 100 )

A_ref_구간 = 현재 레벨 구간의 running-peak (post-peak only)
레벨 구간 = 접촉·자세 변화로 진폭 레벨이 계단식으로 바뀌는 구간
버스트 지표 = 에폭별 영점보정 후 5–15ms 창 p2p, 버스트당 중앙값, Hampel(k=7, 3σ)
```

오프라인 `fes_fatigue_regression.py` 공식의 인과(causal) 버전이다. 실시간에서는 미래를 못 보므로 이동중앙값 95퍼센타일 대신 running-peak를 쓴다.

### 2.2 적응형을 쓴다는 결정의 대가

적응형은 레벨 변화로 인한 계단을 피로로 세지 않기 때문에 **더 보수적**이다. 전수분석에서 전역 기준은 58세션 중 52세션에서 임계 도달을 검출했지만 **적응형은 47세션에서만 검출**했다. 전체 피로도도 전역 44% → 적응형 19%로 절반 이하다.

즉 **적응형만 쓰면 5~6개 세션 중 하나꼴로 자동 종료가 안 걸린다.** 백업 종료 조건이 반드시 필요하다.

| 우선순위 | 종료 조건 |
|---|---|
| 1차 | 적응형 게이지 ≥ 임계, 연속 N버스트 유지 |
| 2차 (백업) | 수축 성공률이 최근 50버스트 기준 초기 대비 40% 이하로 하락 |
| 3차 (백업) | 세션 시간 상한 도달 (권장 15분) |

임계값은 실측 분포(종료 시점 중앙 32%, 최대 중앙 44% — 단 전역 기준)를 적응형 스케일로 환산해 P0에서 확정한다.

### 2.3 신뢰도 게이팅

| 조건 | 앱 동작 |
|---|---|
| events/burst ≥ 10 & 검출률 ≥ 50% | 정상 |
| events/burst < 10 | 게이지 판정 보류 + "센서 확인이 필요해요" |
| 검출 실패 30초 지속 | 자극 중단, 부착 체크 화면으로 |

전수분석에서 88세션 중 58세션만 이 기준을 통과했다. **게이지를 항상 신뢰하면 3분의 1은 틀린 값을 보게 된다.**

### 2.4 실측 데이터가 강제하는 구현 제약

**(a) 자극 직전 구간을 영점 기준으로 쓰지 말 것.** 자극 직전 5샘플이 M-wave 피크의 중앙 20.2% 크기로 오염돼 있고, 74/88 세션에서 10%를 넘었다. 에폭별 영점보정 필수.

**(b) 급등은 피로가 아니라 센서 문제로 분기할 것.** 세션당 평균 4.1회 레벨 변화가 있고, 32초 내 30%p 이상 급등이 13/58 세션에서 나왔다. 게이지 급등 시 앱은 "피로 급증"이 아니라 **"센서 점검 안내"**를 띄운다.

**(c) 절대 피로%를 성과 지표로 삼지 말 것.** 계산 방법에 따라 값이 두 배 이상 갈리고(전역 44 vs 적응형 19), 피험자별 중앙값도 11~66%로 개인차가 피로 차이보다 크다. 웹에서 환자 간 절대% 비교·순위는 금지한다.

### 2.5 선행 필요: 펌웨어 정리

자극 트리거 송신은 불가로 확정됐으므로 앱이 주기를 추정한다. 다만 아래는 여전히 정리돼야 한다.

- `triggerStimulation` OFF의 `delay(2000)` 제거 → 비블로킹 상태머신 (중단 순간 2초간 BLE·계산 공백)
- `DC_OFFSET` 1862 하드코딩 → 세션 시작 무자극 1–2초 자동 캘리브
- 펌웨어 내 SPC 판정·MDF 조건 제거 (무부하 FES에서 RMS 방향이 역전되고, MDF는 자극 하모닉 32/64/96Hz와 혼동됨)
- 펌웨어 역할은 **센싱 + 자극 ON/OFF 실행 + BLE 끊김 시 자동 OFF**로 축소. 판정은 전부 앱

---

## 3. RE-FIT Play (앱)

### 3.1 설계 원칙

첫 실행부터 게임까지 3분 이내. 한 손 조작(주요 버튼은 화면 하단 ⅓, 최소 44pt). 숫자 대신 상태. 소리와 햅틱을 시각과 동등하게 취급. 실패 표현 금지. 오프라인에서도 훈련이 끝까지 되고 나중에 동기화. 중단 버튼은 항상 화면에.

### 3.2 정보 구조

| 탭 | 목적 | 핵심 요소 |
|---|---|---|
| 홈 | 훈련 시작 유도 | 오늘의 훈련 카드, 연속 수행일, 기기 연결 배지 |
| 게임 | 실제 훈련 | 3.3 세션 플로우 |
| 기록 | 지속 동기 | 월 캘린더, 뱃지, 주간 요약 |
| 설정 | 관리 | 기기 페어링, 강도 프로파일, 알림, **치료사 초대 코드 입력** |

### 3.3 세션 플로우

**① 기기 연결** — BLE 자동 재연결, 배터리·신호 세기 표시.

**② 부착 체크**

| 항목 | 판정 |
|---|---|
| EMG 전극 | 무자극 1–2초 baseline noise RMS + DC offset 측정, 임계 초과 시 재부착 안내 |
| 마사지기 패드 | 최저 강도 테스트 펄스 1회 → M-wave 검출 여부 |
| 기기 상태 | 배터리, BLE 신호 |

각 항목에 부착 위치 그림을 붙인다. 텍스트만으로는 매번 실패한다.

**③ 강도 설정 마법사**
낮은 단계부터 올리며 각 단계에서 자동으로 events/burst를 측정하고, 사용자에게 "움직임이 느껴지나요?"(안 느껴짐 / 적당함 / 너무 셈)를 묻는다. **목표는 `events/burst ≥ 10`을 만족하는 최소 강도.** 결과를 프로파일로 저장해 다음 세션 기본값으로 쓰고, 변화 이력은 웹에서 치료사가 본다.

**④ 동기화 + 워밍업 (30초)** — 1.3의 자극 주기 검출 + A_ref 확보. 튜토리얼 라운드로 포장.

**⑤ 게임 플레이** — 1장.

**⑥ 종료**

| 종료 코드 | 트리거 |
|---|---|
| `fatigue_threshold` | 적응형 게이지 임계 + 연속 N버스트 |
| `success_rate_drop` | 수축 성공률 백업 조건 |
| `game_complete` | 스테이지 목표 달성 |
| `timeout` | 시간 상한 |
| `user_stop` | 사용자 중단 버튼 |
| `remote_stop` | **치료사 원격 중단** |
| `signal_lost` | 신뢰도 붕괴 지속 |
| `device_disconnect` | BLE·전원 |

**⑦ 종료 리포트 — 3줄**
"오늘 340번 쥐었어요 / 9분 완주 / 내일도 같은 시간에 만나요"
그래프도 퍼센트도 여기엔 없다.

### 3.4 캘린더·기록

월 캘린더에 점 색 = 그날 게이지 등급, 점 크기 = 반복 횟수. 연속 수행일, 주간 목표 달성률. 세션 상세는 앱에선 반복 횟수 / 수행 시간 / 종료 이유(사용자 언어) 3개만.

뱃지는 첫 완주, 7일 연속, 누적 1만 회 반복처럼 **횟수·지속 기반**으로 만든다. 피로도 수치 기반 뱃지는 개인차가 커서 불공정하므로 만들지 않는다.

### 3.5 안전

강도 상한 하드코딩, 세션 중 하향만 가능. 통증 중단 버튼 상시 노출 → 즉시 자극 OFF. BLE 끊김 시 펌웨어 페일세이프로 자극 자동 OFF(앱이 죽어도 멈춰야 함). 연속 세션 제한과 최소 휴식 간격. 최초 1회 주의사항 동의.

**로컬 자동 종료가 언제나 1차 안전장치다.** 원격 중단은 보조 수단이며, 네트워크에 의존하는 어떤 것도 안전 기능의 1차가 될 수 없다.

### 3.6 로컬 저장

raw는 저장하지 않고 **버스트 단위 지표만** 남긴다. 세션당 약 340버스트 → 수십 KB. SQLite(drift) + `synced` 플래그로 업로드 관리.

---

## 4. RE-FIT Care (웹)

### 4.1 사용자

치료사는 담당 환자 5–30명을 주 1–2회 데스크톱에서 확인한다. 궁금한 건 둘이다 — 누가 안 하고 있나, 누가 이상한가. 관리자는 계정·기기·데이터 내보내기를 맡는다.

### 4.2 화면

**① 로그인 / 권한** — 치료사·관리자 역할 분리, 담당 환자만 조회.

**② 환자 목록 (치료사 홈)**

| 컬럼 | 내용 |
|---|---|
| 환자 | 익명 코드 + 표시명 |
| 최근 세션 | 날짜 + 경과일 |
| 7일 수행률 | 5/7 |
| 최근 반복 횟수 | 340회 |
| 최근 게이지 | 등급 색 + 값 |
| 상태 | 진행 중(라이브) / 정상 / 미수행 / 신호 불량 |

**진행 중인 세션은 목록 최상단에 라이브 배지로 뜬다.**

**③ 라이브 세션 뷰 (실시간)**

앱이 세션 중 2–5초 간격으로 요약 상태를 업로드하고, 웹은 Supabase Realtime으로 구독한다. 전체 시계열은 종료 시 일괄 업로드한다.

| 표시 | 내용 |
|---|---|
| 경과 시간 / 반복 횟수 | 실시간 카운트 |
| 게이지 | 적응형 기준 현재값 + 최근 2분 추이 |
| 수축 성공률 | 최근 50버스트 기준 |
| 신호 품질 | events/burst, 검출률 |
| **원격 중단 버튼** | 아래 4.3 |

**④ 세션 리포트 — 이 웹의 존재 이유**

"어느 정도로 얼마나 피로해져서 멈췄는지"가 여기서 답해진다.

- **게이지 궤적 그래프**: x=경과 시간, y=피로도%. 마커 3종 — onset(첫 임계 도달), 레벨 변화 지점, 종료 시점
- **수축 성공률 궤적**을 같은 축에 겹쳐 표시 (피로 진행이 실제 동작 실패로 이어졌는지 확인)
- **중단 정보 블록**

| 항목 | 예시 |
|---|---|
| 중단 시각 | 시작 후 8분 12초 |
| 중단 시점 게이지 | 19% (적응형) |
| 중단 사유 | `fatigue_threshold` |
| onset 시각 | 시작 후 3분 02초 |
| 총 반복 횟수 | 298회 |
| 수축 성공률 | 초기 96% → 종료 시점 61% |

- **신뢰도 배지**: events/burst 중앙값, 검출률, 레벨 변화 횟수. 낮으면 그래프 위에 경고 오버레이

**⑤ 환자 상세** — 요약 카드(총 세션, 누적 반복 횟수, 4주 수행률, 현재 강도 레벨), 세션 캘린더 히트맵, 추이 차트(세션별 최고/종료 게이지, onset, 반복 횟수), 강도 프로파일 변화 이력.

**⑥ 알림** — 3일 이상 미수행, `signal_lost`·`device_disconnect` 반복, 성공률 급락.

**⑦ 관리자** — 계정, **초대 코드 발급**, 기기 등록, CSV 내보내기, 앱·펌웨어 버전 현황.

### 4.3 원격 중단 설계

치료사가 웹에서 중단을 누르면 서버에 명령이 기록되고, 앱이 실시간 채널로 받아 BLE로 자극을 끈다. 네트워크가 끊겨 있으면 명령이 도달하지 않으므로 **웹에 전달 상태를 반드시 표시**한다.

| 상태 | 웹 표시 |
|---|---|
| 전송됨 | "중단 요청 전송" |
| 앱 수신 확인 | "앱이 요청을 받았습니다" |
| 자극 OFF 확인 | "자극이 중단되었습니다" ← 여기까지 와야 완료 |
| 5초 내 무응답 | **"전달되지 않았습니다. 환자에게 직접 연락하세요"** |

중단 이벤트는 `remote_stop`으로 기록되고, 누가 언제 눌렀는지 남긴다.

### 4.4 중단 사유 분류

치료사가 **"진짜 피로로 멈춘 것"과 "장비 문제로 멈춘 것"**을 구분하지 못하면 그 위의 모든 해석이 오염된다. 색과 아이콘으로 확실히 나눈다.

| 코드 | 색 | 의미 | 치료사 액션 |
|---|---|---|---|
| `fatigue_threshold` | 주황 | 피로 임계 도달 | 정상. 강도·시간 조정 검토 |
| `success_rate_drop` | 주황 | 수축 성공률 하락 | 정상. 전극 상태도 함께 확인 |
| `game_complete` | 초록 | 목표 달성 | 정상. 목표량 상향 검토 |
| `timeout` | 파랑 | 시간 상한 | 강도 상향 검토 |
| `user_stop` | 회색 | 사용자 중단 | 통증·불편 확인 |
| `remote_stop` | 보라 | 치료사 원격 중단 | 사유 메모 필요 |
| `signal_lost` | 빨강 | 신뢰도 붕괴 | 전극 교체·부착 재교육 |
| `device_disconnect` | 빨강 | BLE·전원 | 기기 점검 |

### 4.5 지표 정의

| 지표 | 정의 |
|---|---|
| 반복 횟수 | 수축 성공으로 판정된 버스트 수 |
| 수축 성공률 | 성공 버스트 / 전체 자극 버스트 |
| 최고 피로도 | 세션 내 적응형 게이지 최댓값 |
| 종료 피로도 | 마지막 30초 평균 |
| onset | 게이지가 임계를 처음 넘은 시각 |
| 검출률 | 예상 버스트 대비 M-wave 검출 성공 비율 |
| 레벨 변화 수 | 적응형 baseline 구간 분할 개수 (접촉·자세 변화 대용 지표) |
| 신뢰도 등급 | A(≥20 ev/burst) / B(10–20) / C(<10, 해석 주의) |

### 4.6 해석 규칙

환자 간 절대 피로% 순위는 매기지 않는다. 개인차(중앙 11~66%)가 피로 차이보다 크다. 보고는 **개인 내 시간 변화, onset, 반복 횟수·성공률** 중심으로 한다.

---

## 5. 데이터와 연동

### 5.1 구조

```
현재:  ESP32 ──BLE──> Flutter 앱 (로컬) ── 끝
목표:  ESP32 ──BLE──> Flutter 앱 ──HTTPS/Realtime──> Supabase ──> 웹
                          ↑                              │
                          └────── 원격 중단 명령 ─────────┘
```

**Supabase(Postgres)** 를 쓴다. 세션·버스트 시계열 조회에 SQL이 유리하고, Realtime 구독으로 라이브 뷰와 원격 중단이 추가 인프라 없이 되며, 연구용 CSV 내보내기와 기존 Python 분석 파이프라인이 같은 DB를 직접 읽을 수 있다.

### 5.2 업로드 정책

세션 중에는 요약 상태만 2–5초 간격으로 보내고, 종료 시 버스트 시계열(gzip)과 요약을 일괄 업로드한다. raw 신호는 기본 미업로드이며 연구 모드 + Wi-Fi에서만 옵션. 업로드 실패 시 로컬 큐에 보관 후 재시도.

### 5.3 스키마 초안

| 테이블 | 주요 컬럼 |
|---|---|
| `patients` | id, anon_code, display_name, birth_year, affected_side, paralysis_type |
| `therapists` | id, name, email, org_id |
| `invite_codes` | code, therapist_id, expires_at, used_by_patient_id, used_at |
| `care_relations` | therapist_id, patient_id, granted_at |
| `devices` | id, serial, firmware_version, assigned_patient_id |
| `intensity_profiles` | id, patient_id, level, valid_from, events_per_burst_at_set |
| `sessions` | id, patient_id, device_id, started_at, ended_at, duration_s, game_id, stage_id, intensity_level, **end_reason**, rep_count, success_rate, max_fatigue, end_fatigue, onset_s, burst_count, detect_rate, events_per_burst_median, level_change_count, reliability_grade, stim_period_ms, app_version, fw_version |
| `session_live` | session_id, updated_at, elapsed_s, rep_count, fatigue, success_rate_50, signal_quality (라이브 뷰용 단일 행) |
| `session_series` | session_id, t_s, p2p, fatigue, contraction_ok, valid (버스트 단위) |
| `session_events` | session_id, t_s, type(level_change / signal_warning / user_pause / remote_stop / stim_off), actor, payload |

`sessions` 한 행이 목록·요약 화면 전부를 커버하고, `session_series`는 리포트 그래프에서만 읽는다.

### 5.4 초대 코드 흐름

치료사가 웹에서 코드 발급(6자리, 만료 7일, 1회용) → 환자가 앱 설정에서 입력 → `care_relations` 생성 + 코드 소진. 환자 식별정보는 최소화하고 익명 코드로 저장한다.

---

## 6. 개발 순서

| 단계 | 내용 | 산출물 | 예상 |
|---|---|---|---|
| **P0. 계약 고정** | 게이지 알고리즘 명세, 위상 동기화 허용 오차 실측, 종료 임계 확정, DB 스키마 | 명세 문서 | 1주 |
| **P1. 펌웨어 정리** | delay 제거, DC 자동 캘리브, 판정 로직 제거, BLE 끊김 페일세이프 | 새 펌웨어 | 1주 |
| **P2. 신호 엔진 (Dart)** | 자극 주기 검출·위상잠금, 버스트 지표, Hampel, 적응형 게이지, 수축 판정 | 엔진 모듈 + 오프라인 raw 대조 테스트 | 2–3주 |
| **P3. 앱 준비 플로우** | 부착 체크, 강도 마법사, 동기화 튜토리얼 | 세션 진입 완성 | 2주 |
| **P4. 게임 1종** | 코어 루프 + 스테이지 3개 + 피드백 연출 | 플레이 가능 빌드 | 3–4주 |
| **P5. 백엔드 + 동기화** | Supabase 구축, 업로드 큐, Realtime, 초대 코드 | 서버 + 앱 연동 | 2주 |
| **P6. 웹 MVP** | 로그인, 환자 목록, 세션 리포트, 환자 상세 | 배포된 웹 | 3주 |
| **P7. 실시간·마무리** | 라이브 뷰, 원격 중단, 알림, 관리자, 내보내기 | v1 | 2–3주 |

**병렬 가능:** P2와 P3 동시 진행. P5 이후 P4와 P6 병렬.
**순서상 잠금:** P0의 위상 동기화 허용 오차가 안 나오면 P4의 큐 타이밍을 못 만든다. P2가 안 끝나면 게임의 성공 판정이 없다.

---

## 7. 남은 리스크

**① 위상 드리프트 (최대 리스크).** 자극 트리거가 없어 앱이 주기를 추정하는 구조라, 시간이 갈수록 큐와 실제 수축이 어긋난다. 어긋나면 게임은 돌아가지만 신경가소성 효과가 사라진다 — **증상 없이 효과만 사라지는 종류의 실패**라 더 위험하다. P0에서 실측으로 드리프트 속도를 재고 보정 주기를 정해야 한다.

**② 적응형 기준의 과소검출.** 58세션 중 47세션에서만 임계가 걸렸다. 2.2의 백업 종료 조건 두 개가 반드시 함께 구현돼야 한다.

**③ 회복 검증 미완.** 자극 정지 후 M-wave가 회복되는지 확인된 적이 없어, 이 지표가 실제 근육 상태를 반영한다는 직접 증거가 아직 없다. 제품에서 "피로도"라고 부르기 전에 실험 1건이 필요하다. 실험 없이 MVP를 낼 거라면 앱·웹 문구를 "피로도" 대신 **"반응 저하"** 같은 중립 표현으로 두는 편이 안전하다.

**④ 신호 품질이 전체를 좌우한다.** 88세션 중 58세션만 신뢰 기준을 통과했다. 게임 성공률, 피로 게이지, 웹 리포트가 전부 여기 얹혀 있으므로 **부착 체크와 강도 마법사가 사실상 제품의 절반**이다. 여기에 개발 시간을 아끼면 나머지가 다 무너진다.

**⑤ 인허가.** FES 자극 제어를 앱이 하고 치료사가 원격 중단까지 하는 구조는 의료기기 소프트웨어 해당 가능성이 있다. MVP 완성 후 확인 예정이나, 그 전까지는 연구·시험 목적 한정으로 운용하고 공개 배포는 하지 않는다.

---

## 부록. 설계 결정의 실측 근거

| 결정 | 근거 |
|---|---|
| 게이지 = M-wave 진폭 기반 | 세션 내 분리력 RMS 0.51 / p2p 0.44 vs MDF 0.008 → 피로는 진폭에 있다 |
| MDF 미사용 | 무부하 FES에서 MDF가 자극 하모닉(32/64/96Hz)과 혼동 |
| Hampel 필터 필수 | 글리치 제거 시 최대 피로도 52% → 27.4%로 정정된 사례 |
| 신뢰도 게이팅 | 88세션 중 58세션만 기준 통과 |
| 적응형 백업 조건 필요 | 임계 검출 전역 52/58 vs 적응형 47/58 |
| 절대% 비교 금지 | 전역 44% vs 적응형 19%, 방법 간 차이 중앙 16.1%p, 피험자별 중앙 11~66% |
| 급등 = 센서 의심 분기 | 32초 내 30%p+ 점프가 13/58 세션, 세션당 레벨 변화 4.1회 |
| 에폭별 영점보정 | 자극 직전 5샘플 오염이 M-wave 피크의 중앙 20.2%, 74/88 세션 |
| 게임 템포 1618ms | 버스트 on 591ms / off 1027ms |
| 세션당 약 340회 반복 | 88세션 29,611버스트, 세션 중앙 9.3분 |
