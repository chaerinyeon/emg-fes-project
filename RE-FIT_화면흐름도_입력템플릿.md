# RE-FIT 화면 흐름도 — 작성용 입력 템플릿

레퍼런스처럼 **화면 단위 전체 흐름도**를 그리려면 아래 정보가 필요합니다.
채워서 통째로 저에게 붙여넣어 주세요. **모르는 건 "미정"이라고 쓰시면 됩니다 — 미정도 정보입니다.**
1·2·3번만 채워져도 초안은 나옵니다. 나머지는 정확도를 올리는 항목이에요.

> 이 그림은 28종 목록에서 **11번 메뉴 구성도와 12번 화면 설계서 사이**에 들어갑니다.
> 별도 산출물 번호는 없으니 12번의 첫 장으로 넣는 것을 권합니다.

---

## 복붙용 프롬프트 (템플릿을 다 채운 뒤 맨 앞에 붙이세요)

```
아래 정보를 기준으로 RE-FIT 앱·웹 화면 흐름도를 Figma에 그려줘.
- 역할별로 색을 구분하고, 분기는 마름모, 오버레이는 점선 박스로 표기
- 앱과 웹을 좌우로 나누고 교차 지점은 굵은 화살표로 연결
- 화면 ID(SC-A-xx / SC-W-xx)를 각 박스에 표기
- 미정 항목은 회색 점선 박스로 남겨줘

[여기에 아래 채운 내용 전부 붙여넣기]
```

---

# 1. 역할과 색 구분

흐름도에서 색을 나누는 기준입니다. 레퍼런스에서 공급자(남색)와 수요자(빨강)를 나눈 것과 같은 역할이에요.

| 역할 | 쓰는 제품 | 색(원하는 색 있으면) | 비고 |
|---|---|---|---|
| 환자 | RE-FIT Play (앱) | | 직접 조작 가능? 화면만 보는가? |
| 보호자 | RE-FIT Play (앱) | | 환자 대신 조작하는 범위는? |
| 치료사 | RE-FIT Care (웹) | | |
| 관리자 | ? | | 관리자 화면이 있나요, 없나요? |

**질문 — 환자와 보호자를 계정으로 구분하나요, 아니면 한 계정을 같이 쓰나요?**
답:

---

# 2. 화면 목록 — 앱 (RE-FIT Play)

**가장 중요한 항목입니다.** 아는 만큼만 적으세요. 이름만 있어도 됩니다.

| SC-ID | 화면명 | 무엇을 하는 화면인가 | 어디서 들어오나 | 어디로 나가나 |
|---|---|---|---|---|
| SC-A-01 | 스플래시 | 앱 실행, 로그인 상태 확인 | 앱 아이콘 | 로그인 또는 홈 |
| SC-A-02 | | | | |
| SC-A-03 | | | | |
| | | | | |

**제가 추측하는 앱 화면 목록입니다. 맞으면 그대로 두고, 틀리거나 빠진 게 있으면 고쳐주세요.**

- 스플래시 / 로그인·가입 / 초대코드 입력 / 온보딩·튜토리얼
- 홈(오늘의 세션)
- 디바이스 연결(페어링) / 전극 부착 점검 / 강도 설정
- 세션 준비(동기화 30초) / 게임 플레이 / 일시정지
- 세션 종료 요약 / 세션 이력 / 설정

**질문 — 게임은 1종인가요, 여러 종인가요?** (여러 종이면 게임 선택 화면이 하나 더 생깁니다)
답:

**질문 — 홈 화면이 따로 있나요, 아니면 앱을 켜면 바로 세션 준비로 가나요?**
답:

---

# 3. 화면 목록 — 웹 (RE-FIT Care)

| SC-ID | 화면명 | 무엇을 하는 화면인가 | 어디서 들어오나 | 어디로 나가나 |
|---|---|---|---|---|
| SC-W-01 | 로그인 | 치료사 계정 로그인 | 웹 주소 | 환자 목록 |
| SC-W-02 | | | | |
| | | | | |

**제 추측 목록 — 확인해 주세요.**

- 로그인 / 회원가입·소속 기관 등록
- 환자 목록(대시보드)
- 환자 상세(세션 이력·추이)
- 세션 상세(파형·p2p·피로도 곡선)
- 실시간 라이브 뷰 + 원격 중단
- 리포트 출력 / 초대코드 관리 / 설정

**질문 — 치료사 가입은 자유 가입인가요, 관리자 승인이 필요한가요?**
답:

**질문 — 한 환자를 여러 치료사가 볼 수 있나요?**
답:

---

# 4. 시작점 (앱·웹에 들어오는 모든 경로)

흐름도의 왼쪽 끝에 놓일 것들입니다.

- [ ] 앱 아이콘 실행 (콜드 스타트)
- [ ] 푸시 알림 탭 → 어느 화면으로?
- [ ] 초대코드 링크(딥링크) → 어느 화면으로?
- [ ] 웹 주소 직접 입력
- [ ] 기타:

---

# 5. 분기 조건

"이 조건이면 A 화면, 아니면 B 화면"에 해당하는 것들입니다. 흐름도의 마름모가 됩니다.

| 분기 | 조건 | 참일 때 | 거짓일 때 |
|---|---|---|---|
| 로그인 상태 | 토큰 유효? | 홈 | 로그인 |
| 치료사 연결 | 초대코드로 연결됨? | | 초대코드 입력 |
| 디바이스 페어링 | 이전 기기 기억? | | 페어링 화면 |
| 최초 실행 | 온보딩 봤나? | | 튜토리얼 |
| | | | |

**질문 — 치료사와 연결되지 않은 환자도 혼자 세션을 할 수 있나요?**
답:

**질문 — 디바이스가 연결 안 된 상태로 앱을 둘러볼 수 있나요, 아니면 연결이 필수 관문인가요?**
답:

---

# 6. 오버레이 (화면이 아닌 것)

모달·바텀시트·토스트·전체화면 다이얼로그. 흐름도에서 점선 박스로 표기합니다.

| 이름 | 종류 | 어디서 뜨나 | 무엇을 묻나 |
|---|---|---|---|
| 세션 중단 확인 | 모달 | 게임 플레이 | 정말 중단할지 + 중단 사유 |
| | | | |

---

# 7. 화면 상태 변형

같은 화면인데 상태에 따라 다르게 보이는 것들입니다.

| 화면 | 빈 상태 | 로딩 | 에러 | 오프라인 |
|---|---|---|---|---|
| 세션 이력 | 아직 기록 없음 | | | |
| 환자 목록 | 연결된 환자 없음 | | | |
| | | | | |

**질문 — 앱이 오프라인일 때 세션을 시작할 수 있나요?** (안전 로직은 앱에 있으니 가능할 것 같은데 확인이 필요합니다)
답:

---

# 8. 네비게이션 구조

- **앱**: 하단 탭바가 있나요? 있다면 탭 항목은? / 없다면 어떤 구조인가요?
  답:
- **웹**: 좌측 사이드바인가요, 상단 메뉴인가요?
  답:
- **세션 중 이탈 제한**: 게임 플레이 중에 뒤로가기·홈 이동을 막나요?
  답:
- **백그라운드 복귀**: 세션 중 앱이 백그라운드로 갔다 돌아오면 어떻게 되나요?
  답:

---

# 9. 앱 ↔ 웹 교차 지점

두 제품이 만나는 지점입니다. 흐름도에서 굵은 화살표로 연결됩니다.

| 교차점 | 웹에서 | 앱에서 | 비고 |
|---|---|---|---|
| 초대코드 | 치료사가 발급 | 환자가 입력 → 연결 | 6자리 / 유효기간 / 1회용? |
| 라이브 뷰 | 세션 실시간 조회 | 세션 진행 중 | |
| 원격 중단 | 중단 버튼 | 자극 정지 + 안내 표시 | |
| 리포트 | 출력·다운로드 | 환자도 보나요? | |

**질문 — 환자 앱에서도 지난 세션 리포트를 볼 수 있나요, 치료사 전용인가요?**
답:

---

# 10. 이탈·종료 지점

흐름도의 끝점이 됩니다.

- [ ] 로그아웃 → 어디로?
- [ ] 세션 중단 → 어디로?
- [ ] 계정 삭제 / 연결 해제 → 있나요?
- [ ] 기타:

---

# 11. 알림

| 알림 | 받는 사람 | 언제 | 누르면 어느 화면 |
|---|---|---|---|
| 세션 시작됨 | 치료사 | 환자가 세션 시작 | 라이브 뷰 |
| | | | |

**질문 — 푸시 알림 기능이 MVP 범위에 들어가나요?**
답:

---

# 12. 아직 안 정한 것

여기에 적어주시면 흐름도에 **회색 점선 박스**로 남겨두고 "미정"이라고 표시하겠습니다.
나중에 정해지면 그 박스만 채우면 됩니다.

-
-
-

---

# 부록 A. 제가 이미 알고 있는 것 — 맞는지만 확인해 주세요

아래는 지금까지 대화에서 파악한 내용입니다. **틀린 것만 지적해 주시면 됩니다.**

1. 제품은 3분할 — 디바이스(ESP32, FES+EMG) / RE-FIT Play(환자용 Flutter 앱) / RE-FIT Care(치료사용 웹)
2. 대상은 완전마비 환자의 상지 재활. 환자는 손 조작이 불가하므로 실제 조작 주체는 보호자·치료사
3. 백엔드는 Supabase (Postgres · Auth · Realtime)
4. 치료사–환자 연결은 초대코드 방식
5. 세션은 페어링 → 부착 점검 → 강도 설정 → 30초 동기화 → 라운드 반복 → 종료 순
6. 세션 종료 경로는 3가지 — 자동 종료(3중 조건) / 수동 종료 / 치료사 원격 중단
7. 앱은 상태만 보여주고 파형·수치는 웹에서만 표시
8. 피로 게이지 계산은 앱에서 수행

**틀린 항목 번호와 수정 내용:**

---

# 부록 B. 시간이 없다면 이것만

아래 세 가지만 주셔도 초안은 그릴 수 있습니다.

1. **앱 화면 이름 목록** (순서 없이 나열해도 됩니다)
2. **웹 화면 이름 목록**
3. **각 화면에서 어느 화면으로 갈 수 있는지** — `A → B, C` 형식으로 나열

예시
```
앱: 스플래시 → 로그인 / 홈
    홈 → 세션준비, 이력, 설정
    세션준비 → 페어링 → 부착점검 → 강도설정 → 동기화 → 게임 → 종료요약 → 홈
웹: 로그인 → 환자목록 → 환자상세 → 세션상세
    환자목록 → 라이브뷰 → (원격중단) → 환자목록
```
